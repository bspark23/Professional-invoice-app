
import React from "react";

/**
 * This component is no longer needed since we want to show the landing page first
 */
const RequireLandingPage = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default RequireLandingPage;
